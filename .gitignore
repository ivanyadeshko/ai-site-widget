node_modules/
dist/
.env
*.log
backend/coverage/
# site/ — отдельный npm-проект (Payload 3 + Next), не воркспейс (D-10).
# Артефакты сборки и СОБРАННЫЙ конфиг Payload в репозиторий не едут:
# payload.config.mjs генерируется на билде (`npm run build:payload-config`).
site/.next/
site/payload.config.mjs
site/tsconfig.tsbuildinfo
site/public/media/
# e2e/ — отдельный npm-проект (Playwright тянет браузеры postinstall, D-11):
# свой node_modules и артефакты прогонов в репозиторий не едут.
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
e2e/blob-report/
e2e/.cache/
